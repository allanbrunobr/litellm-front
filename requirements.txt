requests>=2.28.0
python-dateutil>=2.8.2
prisma>=0.9.1  # Cliente Prisma para Python
litellm>=1.0.0  # Opcional, usado apenas para desenvolvimento ou teste local
# Tkinter já vem com o Python padrão, não é necessário instalar via pip 